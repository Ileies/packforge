/**
 * Einheitlicher Hinweis, wenn eine Liste leer bleibt und der Nutzer keine Pakete anlegen darf
 * (`CREATE_SCRIPTS` — z. B. Script Maker / erste Einträge).
 */
export const EMPTY_STATE_NO_CREATE_SCRIPTS_DE =
	'Sie verfügen nicht über die Berechtigung, neue Pakete anzulegen. Wenden Sie sich bei Bedarf an die IT oder an eine Person mit der Rolle Administrator.';

/** Kurztext für leere Suchergebnisse in Listen (Ton wie Software-Bibliothek / Stammdaten). */
export const EMPTY_STATE_SEARCH_ADJUST_DE =
	'Passen Sie die Suche an, oder setzen Sie Filter und Sortierung zurück, um wieder Treffer zu sehen.';
