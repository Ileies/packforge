import { CSRF_COOKIE_NAME } from '$lib/app/brand';

/** Muss mit `session-cookies.ts` übereinstimmen (Client + Server). */
export { CSRF_COOKIE_NAME };
export const SESSION_COOKIE_NAME = 'authToken';
